services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: fit4110/iot-ingestion:v0.1.0-team-iot
    container_name: fit4110-api-lab05
    env_file:
      - .env
    environment:
      APP_MODULE: iot_app.main:app
      APP_PORT: 8000
      AI_SERVICE_URL: http://ai-service:9000
      DB_HOST: db
      DB_PORT: 5432
      SERVICE_NAME: iot-ingestion
      SERVICE_VERSION: v0.1.0-team-iot
    depends_on:
      db:
        condition: service_healthy
      ai-service:
        condition: service_healthy
    networks:
      - team-internal
    ports:
      - "${APP_PORT:-8000}:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  notification:
    build:
      context: .
      dockerfile: Dockerfile
    image: fit4110/notification:v0.1.0-team-notify
    container_name: fit4110-notification-lab05
    env_file:
      - .env
    environment:
      APP_MODULE: notification_app.main:app
      APP_PORT: 8002
      DB_HOST: db
      DB_PORT: 5432
      SERVICE_NAME: notification
      SERVICE_VERSION: v0.1.0-team-notify
    depends_on:
      db:
        condition: service_healthy
    networks:
      - team-internal
    ports:
      - "${NOTIFICATION_PORT:-8002}:8002"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8002/health', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  db:
    image: postgres:15-alpine
    container_name: fit4110-db-lab05
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lab05}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lab05pass}
      POSTGRES_DB: ${POSTGRES_DB:-iotdb}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - team-internal
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lab05} -d ${POSTGRES_DB:-iotdb}"]
      interval: 10s
      timeout: 5s
      retries: 5

  ai-service:
    build:
      context: .
      dockerfile: Dockerfile.ai
    image: fit4110/ai-service:v0.1.0-team-iot
    container_name: fit4110-ai-lab05
    env_file:
      - .env
    networks:
      - team-internal
    ports:
      - "${AI_PORT:-9000}:9000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:9000/health', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  team-internal:
    driver: bridge

volumes:
  db-data:
